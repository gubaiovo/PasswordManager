# --- UI Framework (Flet) ---
flet==0.28.3
flet-runtime==0.28.3 
# 注意：打包时通常不需要 flet-cli 等开发工具，但 pyinstaller 需要 flet 核心库

# --- Networking (HTTP Requests) ---
requests==2.32.5
urllib3==2.5.0
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11

# --- Local Database (SQLite via SQLModel) ---
sqlmodel==0.0.27
sqlalchemy==2.0.44
greenlet==3.2.4

# --- Security & Encryption (Client Side) ---
cryptography==46.0.3
cffi==2.0.0
pycparser==2.23

# --- Data Models & Configuration ---
pydantic==2.12.4
pydantic-core==2.41.5
pydantic-settings==2.12.0
python-dotenv==1.2.1
typing-extensions==4.15.0
annotated-types==0.7.0

# --- Utilities & Packaging ---
packaging==25.0
six==1.17.0
python-dateutil==2.9.0.post0